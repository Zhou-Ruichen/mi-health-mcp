{
  "name": "mi-health-mcp",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "description": "Xiaomi Fitness MCP server with robust personal-baseline analysis for Cloudflare Workers",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "wrangler": "^4.125.0"
  },
  "dependencies": {
    "simple-statistics": "^7.10.2"
  }
}
