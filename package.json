{
  "name": "mi-health-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency Xiaomi Fitness self and relatives MCP server for Cloudflare Workers",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
